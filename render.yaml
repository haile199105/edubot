services:
  - type: web
    name: edubot
    runtime: python
    rootDir: bot
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: WEBHOOK_URL
        sync: false
      - key: PORT
        value: "10000"
